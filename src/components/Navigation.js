import { NavLink } from "react-router-dom";

const Navigation = () => {
    return (
        <nav>
            <NavLink
                to="/home" 
            >Home</NavLink>
        </nav>
    )
}

export default Navigation;